import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:giphyfetch/screens/home_screen.dart';
import 'package:giphyfetch/screens/offline_screen.dart';

main() {
  group('Return child depending on network status', () {
    test('Return offline screen if network status offline', () {});
    test('Return home screen if network status online', () {});
  });
}
